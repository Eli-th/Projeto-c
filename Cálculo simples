#include <stdio.h>
 
int main () {
    int A, B, C, D;
    float E, F, total;
    
    scanf ("%d %d %f %d %d %f",&A,&B,&E,&C,&D,&F);
    
    total = (E * B) + (F * D);
    
    printf ("VALOR A PAGAR: R$ %.2f\n",total);
    return 0;
}
